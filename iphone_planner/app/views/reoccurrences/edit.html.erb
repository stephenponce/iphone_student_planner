<h1>Editing reoccurrence</h1>

<% form_for(@reoccurrence) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'fields', :locals => {:f => f}%>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @reoccurrence %> |
<%= link_to 'Back', reoccurrences_path %>
